# Editor / joinbot（已移除）

`joinSession` 桌面 Editor Bot 已从 Bridge 删除。值班只跑 LaunchAgent 无头守护。

- 运行时：[`headless-daemon.md`](./headless-daemon.md)
- Copilot CLI/SDK 钉在 `runtime/`：[`../runtime/README.md`](../runtime/README.md)
